require 'spec_helper'

describe PerfectRetry do
  it 'has a version number' do
    expect(PerfectRetry::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
